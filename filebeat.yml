filebeat:
  prospectors:
    -
      paths:
        - /mnt/logs/birdeye/error*
      input_type: log
      document_type: paiduilogs
  registry_file: /var/lib/filebeat/registry
output:
  logstash: # not "elasticsearch"!
    hosts: ["logstash.birdeye.com:5043"] # change to your hostname, note change of port
shipper:
logging:
  files:
